# 免费AI平台快速测试指南

## 🚀 5分钟快速测试

### 方法1：命令行测试（最快）

**GitHub Models（已配置，立即可用）**
```bash
curl -X POST https://models.inference.ai.azure.com/chat/completions \
  -H "Authorization: Bearer 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" \
  -H "Content-Type: application/json" \
  -d '{"model": "gpt-4o-mini", "messages": [{"role": "user", "content": "你好，请介绍一下自己"}]}'
```

### 方法2：Python测试

**创建测试文件**：
```python
# test_api.py
from openai import OpenAI

# GitHub Models（已配置）
client = OpenAI(
    base_url="https://models.inference.ai.azure.com",
    api_key="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"
)

# 测试不同模型
models = ["gpt-4o-mini", "gpt-4o", "DeepSeek-V3-0324"]

for model in models:
    print(f"\n测试模型: {model}")
    response = client.chat.completions.create(
        model=model,
        messages=[{"role": "user", "content": "你好，请用一句话介绍自己"}]
    )
    print(f"响应: {response.choices[0].message.content}")
```

**运行测试**：
```bash
python test_api.py
```

### 方法3：网页平台测试

**直接访问测试**：
1. **LMArena**: https://lmarena.ai - 多模型对比
2. **XSimple Chat**: https://xsimplechat.com/chat - 聚合平台
3. **蓝镜ChatGPT**: https://ai.lanjingchat.com - 中文友好

---

## 📊 测试清单

### ✅ GitHub Models（已配置）
- [ ] 测试 GPT-4o-mini
- [ ] 测试 GPT-4o
- [ ] 测试 DeepSeek-V3
- [ ] 测试 Llama 4

### ⏳ 其他平台（需注册）
- [ ] OpenRouter
- [ ] Google AI Studio
- [ ] Cloudflare Workers AI

---

## 🎯 测试目标

**验证项目**：
1. API是否可用
2. 响应速度如何
3. 回答质量如何
4. 是否有频率限制

**测试用例**：
```python
test_cases = [
    "你好，请介绍一下自己",
    "用Python写一个快速排序算法",
    "解释什么是大语言模型",
    "写一段简单的爬虫代码"
]
```

---

## 📝 测试报告模板

```markdown
## 平台测试报告

**测试人**: [你的名字]
**测试时间**: [时间]
**测试平台**: [平台名称]

### 测试结果
- GPT-4o-mini: ✅ 可用 / ❌ 不可用
- DeepSeek-V3: ✅ 可用 / ❌ 不可用
- Llama 4: ✅ 可用 / ❌ 不可用

### 性能评估
- 响应速度: ⭐⭐⭐⭐⭐
- 回答质量: ⭐⭐⭐⭐⭐
- 稳定性: ⭐⭐⭐⭐⭐

### 问题记录
- [记录遇到的问题]

### 建议
- [你的建议]
```

---

## 🔧 快速测试脚本

**一键测试所有模型**：
```bash
#!/bin/bash
# test_all_models.sh

TOKEN="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"
API="https://models.inference.ai.azure.com/chat/completions"

echo "开始测试GitHub Models..."

# 测试 GPT-4o-mini
echo -e "\n=== 测试 GPT-4o-mini ==="
curl -s -X POST $API \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"model": "gpt-4o-mini", "messages": [{"role": "user", "content": "你好"}]}' \
  | jq -r '.choices[0].message.content'

# 测试 DeepSeek-V3
echo -e "\n=== 测试 DeepSeek-V3 ==="
curl -s -X POST $API \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"model": "DeepSeek-V3-0324", "messages": [{"role": "user", "content": "你好"}]}' \
  | jq -r '.choices[0].message.content'

# 测试 Llama 4
echo -e "\n=== 测试 Llama 4 ==="
curl -s -X POST $API \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"model": "Llama-4-Scout-17B-16E-Instruct", "messages": [{"role": "user", "content": "你好"}]}' \
  | jq -r '.choices[0].message.content'

echo -e "\n测试完成！"
```

**运行脚本**：
```bash
chmod +x test_all_models.sh
./test_all_models.sh
```

---

## 💡 测试建议

1. **先测试GitHub Models**（已配置，立即可用）
2. **记录测试结果**（使用上面的模板）
3. **分享发现的问题**（在群里反馈）
4. **提出优化建议**（帮助改进）

---

## 📞 遇到问题？

**常见问题**：
1. API调用失败 → 检查Token是否正确
2. 响应超时 → 重试或切换模型
3. 频率限制 → 等待后重试

**反馈渠道**：
- 在群里@我
- 查看文档：`免费LLM-API注册指南.md`

---

**开始测试吧！有问题随时反馈！** 🚀